part of 'app_pages.dart';

abstract class Routes {
  Routes._();

  static const home = AppRouteNames.home;
  static const practice = AppRouteNames.practice;
  static const favorites = AppRouteNames.favorites;
  static const statistics = AppRouteNames.statistics;
  static const colorSchemes = AppRouteNames.colorSchemes;
  static const speechServices = AppRouteNames.speechServices;
  static const dataBackup = AppRouteNames.dataBackup;
  static const pronunciation = AppRouteNames.pronunciation;
}

abstract class _Paths {
  _Paths._();

  static const home = AppRouteNames.home;
  static const practice = AppRouteNames.practice;
  static const favorites = AppRouteNames.favorites;
  static const statistics = AppRouteNames.statistics;
  static const colorSchemes = AppRouteNames.colorSchemes;
  static const speechServices = AppRouteNames.speechServices;
  static const dataBackup = AppRouteNames.dataBackup;
  static const pronunciation = AppRouteNames.pronunciation;
}
